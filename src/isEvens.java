public class isEvens {
    public boolean isEven(int num) {
        // TODO:
        if (num == 0 ){
            return  true;
        }
        else {
            if (num % 2 == 0) {
                return true;
            } else return false;
        }
    }
}
